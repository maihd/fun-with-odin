## Conclusion
- C function can return struct, and make Odin foreign C function have multiple returns
- And vice versa, when we export Odin function have multiple returns, just treat result as a struct